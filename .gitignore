# Mac 
.DS_Store

# Test binaries
*.test
tmp

# Coverage
*.out

# Binaries
bin
dist
# Build output binaries (not source directories)
/devradar
/devradar-serve
/devradar-scan

# Keys
*.pem

# GitHub Actions WIF credential file (written into the workspace by
# google-github-actions/auth; must be ignored so goreleaser doesn't see the
# tree as dirty).
gha-creds-*.json

# SBOMs
.sboms

# Configs
config.yaml

# Worktrees
.worktrees

# Spike / scratch (SBOM generation experiments; fixtures live in pkg/sbom/testdata)
.spike/


# =============================================================================
# Terraform
# =============================================================================
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Per-developer Claude Code overrides (local only)
.claude/settings.local.json
# Terraform partial backend config (deployment-specific)
backend.hcl
